#include <stdio.h>

#include "test_common.h"

void dummy_function() {
    // Do nothing (to check if two different files that include
    // test_common.h can be linked well without any conflicts).
}

